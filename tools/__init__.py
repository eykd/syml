"""Developer tooling for the syml repository.

Stdlib-only helpers invoked as ``.venv/bin/python -m tools.<module>``: the
Claude Code Bash guard hook, the ralph RED self-commit, and the harden
fingerprint writer. Covered by mypy, ruff, and the 100% coverage gate like
``src/``.
"""
