name: Continuous integration
on: push

jobs:
  ci:
    # This workflow will install Python dependencies, run tests and
    # lint with a variety of Python versions For more information see:
    # https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Set up uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Set up just
      uses: extractions/setup-just@v2
    - name: Install dependencies
      run: |
        uv sync --all-groups
    - name: Lint with ruff
      run: |
        uv run python -m ruff check
    - name: Check types
      run: |
        uv run python -m mypy
    - name: Test with pytest
      run: |
        uv run python -m pytest -vv --disable-warnings --cov-fail-under=100
      env:
        PYTHONDONTWRITEBYTECODE: 1
    # Feature branches are expected to fail this until their step bindings land;
    # master only ever receives hardened branches, so it stays green there.
    - name: Acceptance tests (pytest-bdd)
      run: |
        just acceptance
      env:
        PYTHONDONTWRITEBYTECODE: 1
